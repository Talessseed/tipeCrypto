void AES_Encrypt (unsigned char* message, unsigend char* key)
{
unsigned char state[16];
for (int i = 0; i < 16; ++i)
    state[i] = message[i]
    
int numberOfRounds = 1;

KeyExpansion();
AddRoundKey(state, key); // Whitening/AddRoundKey

for (int i = 0
